Const l =20 ;
Const max =50 ;
Let valori = [] ; // inizializzo l’array 
Let contatore = 0;
Let trovato ; //  per controllare se ci sono doppi 
Let estratto ;
//ciclo 
Do {
	trovato =0 ; // lo inizializzo qua perché a ogni ciclo si azzera 
	floor //estraggo il numero 

	//verifica se il numero è presente 
	for(i=0 ; i<=contatore ;i++){
		if(estratto== valori[I] ){
		trovato=1; // se è presente lo stesso valore esce dal if e continua con l’array 
		break;
		}
	}
	// inserisco il numero dell’array  se non è presente 
	
} while(contatore <= l ) 
// DA FINIRE 
